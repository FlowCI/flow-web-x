import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class JobLogsView extends Component {
  static propTypes = {
    job: PropTypes.object
  }

  render () {
    return <div>
        hello world
    </div>
  }
}
